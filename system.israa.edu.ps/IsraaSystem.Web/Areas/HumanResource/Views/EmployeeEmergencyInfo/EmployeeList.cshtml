@using System.Data
@model IsraaSystem.Core.Classic.HumanResource.EmployeeEmergencyInfo.EmployeeEmergencyInfoVM

@{
    Layout = null;
}

<div id="employee-page">
    @if (TempData["toast-msg"] != null)
    {
        int status = Convert.ToInt32(TempData["toast-msg"].ToString().Split(';')[0]);
        String msg = TempData["toast-msg"].ToString().Split(';')[1];
        <script type="text/javascript">
            showToast("@msg", "@((status > 0) ? "success":"error")");
        </script>
    }
    <script type="text/javascript">
        setMenu('#menu-employee-emergency-info');
    </script>
    <div class="row">
        <div class="col-md-12">
            <div class="portlet light bordered">
                <div class="portlet-title">
                    <div class="caption">
                        <i class="icon-globe theme-font hide"></i>
                        <span class="caption-subject font-blue-madison bold uppercase">بيانات الطوارئ</span>
                    </div>
                    <div class="actions">
                        <a href="/HumanResource/EmployeeEmergencyInfo/EmployeeAdd?empId=@Model.filter.EmployeeID" class="btn btn-primary pop-up-lg">إضافة بيانات جديدة</a>
                    </div>
                </div>
                <div class="portlet-body">
                    <div class="row">
                        <div class="col-md-12">
                            @if (Model.data != null && Model.data.Rows.Count > 0)
                            {
                                <table class="table table-striped table-hover">
                                    <thead>
                                        <tr>
                                            <th class="wp5">#</th>
                                            <th class="wp30">الإسم رباعي</th>
                                            <th class="wp25 text-center">صلة القرابة</th>
                                            <th class="wp15 text-center">الجوال</th>
                                            <th class="wp15 text-center">العنوان</th>
                                            <th class="10"></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (DataRow row in Model.data.Rows)
                                        {
                                            <tr>
                                                <td>@row["RowNo"]</td>
                                                <td>@row["Name"]</td>
                                                <td class="text-center">@row["RelativeType"]</td>
                                                <td class="text-center">@row["Mobile"]</td>
                                                <td class="text-center">@row["AddressDetails"]</td>
                                                <td class="text-center">
                                                    <a href="/HumanResource/EmployeeEmergencyInfo/EmployeeEdit/@row["ID"]" class="btn btn-sm btn-primary pop-up-lg"><i class="fa fa-edit"></i> تعديل</a>
                                                    <a href="/HumanResource/EmployeeEmergencyInfo/Delete/@row["ID"]" class="btn btn-sm btn-danger confirm" data-title="حذف بيانات الطوارئ" data-msg="هل أنت متأكد من أنك تود حذف بيانات الطوارئ ؟" data-action="حذف" data-ajax-update="#employee-page"><i class="fa fa-times"></i> حذف</a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            }
                            else
                            {
                                <div class="alert alert-info alert-dismissible text-center" role="alert">
                                    <span>لا يوجد بيانات لعرضها</span>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>