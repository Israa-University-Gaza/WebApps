@using IsraaSystem.Application.Helper
@model IsraaSystem.Application.Repository.materialType.MaterialTypeDto
@{
    Layout = null;
}

<div id="add-material-type" class="modal-content">

    @{
        AjaxOptions opt = new AjaxOptions
        {
            InsertionMode = InsertionMode.ReplaceWith,
            HttpMethod = "POST",
            UpdateTargetId = "add-material-type",
            OnSuccess = "ModelHideFormSubmit"
        };
    }
    @Html.Partial("_Alerts")
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title">إضافة تصنيف جديد</h4>
    </div>
    <div class="modal-body modal-form">
        <div class="portlet-body form">
            @using (Ajax.BeginForm("Add", "MaterialType", opt, new { @class = "form-horizontal" }))
            {
                @Html.AntiForgeryToken()
                <div class="form-body">

                    <div class="form-group">
                        <label class="col-md-3 control-label">اسم التصنيف</label>
                        <div class="col-md-8">
                            @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger help-block" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-md-3 control-label">تابع للتصنيف</label>
                        <div class="col-md-8">
                            @Html.HiddenFor(model => model.ParentID)
                            @Html.DropDownListFor(model => model.ParentID, DropDownHelpper.MaterialTypes(), "اختر ", new { @class = "form-control select2" , @disabled = "disabled" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-md-3 control-label">فعال</label>
                        <div class="col-md-8">
                            @Html.CheckBoxFor(model => model.IsActive, new { @data_toggle = "toggle", @class = "toggle-checkbox" })
                        </div>
                    </div>
                </div>
                <div class="form-actions">
                    <div class="row">
                        <div class="col-md-offset-3 col-md-8">

                            <input id="submit" type="submit" class="btn btn-primary" value="حفظ">
                            <button type="button" class="btn btn-danger" data-dismiss="modal">إلغاء</button>

                        </div>
                    </div>
                </div>
            }
        </div>
    </div>

</div>


<script>
    $.validator.unobtrusive.parse($("form"));

    $(function () {

        $('.toggle-checkbox').bootstrapToggle();

    });

    function ModelHideFormSubmit() {
        $('.modal').modal('hide');
        //$("#tree_2").load("/Repository/MaterialType/MaterialTypeList");
        //window.location.href = '/Repository/MaterialType/MaterialTypeList'
        
        //$("#tree_2").jstree(true).settings.data.url ='/Repository/MaterialType/MaterialTypeList';
        //$('#tree_2').jstree(true).refresh();
    }

    //jQuery(document).ready(function () {
    //    if ($('#ParentID').val() == null) {
    //        $('#ParentID').attr('disabled', 'disabled');
    //    } else {
    //        $('#ParentID').attr('disabled', 'disabled');
    //    }

    //});
</script>