@using IsraaSystem.Application.Helper
@model IsraaSystem.Application.Repository.export.ExportDto

@{
    Layout = null;
}

<div id="add-Export" class="modal-content">

    @{
        AjaxOptions opt = new AjaxOptions
        {
            InsertionMode = InsertionMode.ReplaceWith,
            HttpMethod = "POST",
            UpdateTargetId = "add-Export",
            OnSuccess = "ModelHideFormSubmit"
        };
    }
    @Html.Partial("_Alerts")
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title">صرف مواد من المخزن</h4>
    </div>
    <div class="modal-body modal-form">
        <div class="portlet-body form">
            @using (Ajax.BeginForm("Add", "Export", opt, new { @class = "form-horizontal" }))
            {
                @Html.AntiForgeryToken()
                <div class="form-body">

                    <div class="form-group">
                        <label class="col-md-3 control-label">اسم الصنف</label>
                        <div class="col-md-8">
                            @Html.DropDownListFor(model => model.MaterialId, DropDownHelpper.Materials(), "اختر الصنف", new { @class = "form-control select2" })
                            @Html.ValidationMessageFor(model => model.MaterialId, "", new { @class = "text-danger help-block" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-md-3 control-label">الكمية</label>
                        <div class="col-md-8">
                            @Html.TextBoxFor(model => model.Quantity, new { @class = "form-control", @readonly = "readonly" })
                            @Html.ValidationMessageFor(model => model.Quantity, "", new { @class = "text-danger help-block" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-md-3 control-label">تاريخ التصدير</label>
                        <div class="col-md-8">
                            @Html.TextBoxFor(model => model.DateOfExport, new { @class = "form-control date-picker" })
                            @Html.ValidationMessageFor(model => model.DateOfExport, "", new { @class = "text-danger help-block" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-md-3 control-label">فعال</label>
                        <div class="col-md-8">
                            @Html.CheckBoxFor(model => model.IsActive, new { @data_toggle = "toggle", @class = "toggle-checkbox" })
                        </div>
                    </div>
                </div>
                <div class="form-actions">
                    <div class="row">
                        <div class="col-md-offset-3 col-md-8">

                            <input id="submit" type="submit" class="btn btn-primary" value="حفظ">
                            <button type="button" class="btn btn-danger" data-dismiss="modal">إلغاء</button>

                        </div>
                    </div>
                </div>
            }
        </div>
    </div>

</div>


<script>
    $(function () {

        $('.toggle-checkbox').bootstrapToggle();

    });

    function ModelHideFormSubmit () {
        $('.modal').modal('hide');

    }

    //$(function() {
    //    $('.MaterialId').bind('change', function(ev) {
    //        var value = $(this).val();
    //        $.ajax({
    //            url:'/Repository/Export/test',
    //            type:"POST",
    //            datatype:"json",
    //            OnSuccess:
    //        data: function (d) {
    //            //d.EmployeeNo = $('#EmployeeNo').val();
    //        }
    //    });
    //});
    //});


    var ddl = $("#MaterialId");

      ddl.change(function (e) {
          $.ajax({
              type: "POST",
              url: "/Repository/Export/RemainQuantityTotal?id=" + ddl.val(),
              contentType: "application/json; charset=utf-8",
              data: '{}',
              dataType: "json",
              success: function (response) {
                  $("#Quantity").val(response.hamza);
              },
              error: function (xhr, status, error) {
                  $("#Quantity").val("حدث خطأ ما !");
              }
          });
      });

    $(document).ready(function () {
       
        $('.date-picker').datepicker();

    });

</script>